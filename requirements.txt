# Lazy Podcast Mate runtime + test dependencies.
# NOTE: ffmpeg is also required as a system dependency (not pip-installable).
#   - macOS:   brew install ffmpeg
#   - Ubuntu:  sudo apt install ffmpeg
#   - Windows: https://ffmpeg.org/download.html

python-dotenv>=1.0.0,<2.0.0
pydub>=0.25.1,<0.26.0
# pydub uses the `audioop` stdlib module, which was removed in Python 3.13.
# `audioop-lts` is the PEP 594 replacement and is only needed on 3.13+.
audioop-lts>=0.2.1; python_version >= "3.13"
requests>=2.31.0,<3.0.0
PyYAML>=6.0,<7.0
mutagen>=1.47.0,<2.0.0

# Test dependencies
pytest>=7.4.0,<9.0.0
pytest-mock>=3.11.0,<4.0.0
